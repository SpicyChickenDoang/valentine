// kb//00_KB_ROUTER.txt (concatenated into cache with the domain-specific agent system prompt)

"""
KB ROUTING RULES — read before any DEPTH 2 response.

DEPTH 0 → load NO files. Return [].
          Exception: hard-stop prefilter hits are ALWAYS loaded by Node before this router runs.
DEPTH 1 → load ONLY the single most directly relevant file (1 file max).
          Hard-stop prefilter hits also apply — they are additive.
DEPTH 2 → evaluate ALL signals below. Load the UNION of matched files.
          No arbitrary file count limit. Load what is genuinely needed.
          The only constraint: do not load files with ZERO signal match.

SIGNALS (additive — each matching signal adds its files):

labs_submitted            → [66_LAB_ANALYSIS_ENGINE, 65_BIOMARKER_ATLAS, 35_LAB_INTERPRETATION]
iron / ferritin / microcytosis / thalassemia / recurrent_infusions
                          → [77_IRON_METABOLISM_THALASSEMIA]
terrain_assessment_needed → [61_TERRAIN_ASSESSMENT]
multi_system / DEPTH_2    → [78_CLINICAL_REASONING_BRIDGES]
anomaly_>2x_upper_limit   → [62_CLINICAL_REASONING_ANOMALIES]
calcitriol_reported       → [00_CRITICAL_REFERENCE, 78_CLINICAL_REASONING_BRIDGES]

male_≥45                  → [17_MALE_HORMONES]
female_≥48                → [18_FEMALE_HORMONES]
thyroid_signal            → [57_THYROID_COMPLETE]
fatigue_chronic           → [09_HPA_ADRENAL, 07_MITOCHONDRIA]
sleep_complaint           → [14_SLEEP]
gut_signal                → [12_GUT]
cognitive_signal          → [15_COGNITIVE]
cardiovascular_signal     → [19_CARDIOVASCULAR]
joint_msk_signal          → [20_JOINTS_MSK]
weight_metabolic          → [06_METABOLIC, 29_WEIGHT_BODY_COMP]

oncology_context          → [25_ONCOLOGY_SUPPORT, 11_IMMUNE]
peptides_mentioned        → [33_PEPTIDES]
iv_therapy_mentioned      → [32_IV_THERAPIES]
nad_clinical_context      → [08_LONGEVITY_NAD]
specific_monograph        → [exact file id from index]

PRIORITY TRIAGE (only if token window becomes a concern — clinical edge cases):
1. Files containing active contraindications for THIS patient
2. Core clinical files (labs engine, terrain)
3. System-specific files
4. Adjacent/contextual files

OUTPUT FORMAT (mandatory — never modify this rule):
Output ONLY valid JSON on a single line. No preamble. No markdown. No explanation.
Format: {"always_load":[],"fetch":[],"reasons":[]}
- always_load: IDs that must load on every DEPTH 2 request (global policies)
- fetch: IDs selected for this specific request based on signals above
- reasons: array of short strings explaining each fetch decision (for logs)
If no files needed: {"always_load":[],"fetch":[],"reasons":["DEPTH_0_1_no_kb"]}
"""